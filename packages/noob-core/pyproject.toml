[project]
name = "noob-core"
description = "Optimized rust core for the noob scheduler"
authors = [
    {name = "raymondwjang", email = "ray0815@ucla.edu"},
    {name= "sneakers-the-rat", email = "sneakers-the-rat@protonmail.com"}
]
version = "0.1.0"
# optional accelerator: only works alongside noob (whose domain types the
# rust core and the python adapters are both defined in terms of)
dependencies = ["noob"]
requires-python = ">=3.11"
readme = "README.md"
license = {text = "MIT"}
keywords = [
    "pipeline",
    "pipelines",
    "data-processing",
    "graph-processing",
    "topological-sort",
    "rust",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Rust",
    "Typing :: Typed"
]

[project.urls]
homepage = "https://noob.readthedocs.io"
documentation = "https://noob.readthedocs.io"
repository = "https://github.com/miniscope/noob"

[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[tool.maturin]
# mixed layout: the rust extension is the inner module `noob_core._core`,
# and `python/noob_core/__init__.py` adds the python adapters on top of it
module-name = "noob_core._core"
python-source = "python"
features = ["pyo3/extension-module"]
# an accelerator package is pointless unoptimized -
# build release even for editable installs
profile = "release"
