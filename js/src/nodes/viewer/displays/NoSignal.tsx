/**
 * Placeholder shown when no signal is wired to the required `in` handle.
 * Blinking phosphor "NO SIGNAL" text.
 */

export default function NoSignal() {
  return (
    <div className="viewer-no-signal">
      <span>NO SIGNAL</span>
    </div>
  );
}
